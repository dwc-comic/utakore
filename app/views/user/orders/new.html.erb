<div class="row">
	<h2>購入確認ページ</h2>

	<% @cart.cart_items.each do | cart_item| %>
	<div class="col-lg-4">
		<p><%= attachment_image_tag cart_item.item, :image, :fill, 100, 100, format:'jpeg' %></p>
		<p><%= attachment_image_tag cart_item.item, :image, :fill, 100, 100, format:'jpeg' %></p>
		<p class="text">タイトル : <%= cart_item.item.title %></p>
		<p class="text">アーティスト名 : <%= cart_item.item.artist_name %></p>
	    <p class="text">価格 : <%= cart_item.item.item_price %></p>
	    <p class="text"><%=(cart_item.cart_quantity) %></p>
	</div>
　　　<% end %>


   総計：<%= (@cart.total_price(@cart)).to_i %>円

    <h2 class="text">お届け先情報</h2>
	<p class="text">ご購入者様 : <%= @user.email %></p>
	<p class="text">お届け先ご住所</p>
	<p class="text"><%= @user.postal_code %></p>
	<p class="text"><%= @user.home_address %></p>

   <%= link_to '購入',user_orders_path, method: :post %>


</div>