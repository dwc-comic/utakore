 <div class="form">
    <%= f.text_field :disc_number,:placeholder => "ディスク番号", class: "field_disc" %>
    <%= link_to_remove_association "取消", f %>
  </div>

  <div class="items">
    <%= f.fields_for :songs do |song| %>
      <%= render 'song_fields', f: song %>
    <%end%>
    <div class="links">
      <%= link_to_add_association "曲追加", f, :songs %>
    </div>
  </div>
